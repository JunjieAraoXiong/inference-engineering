# Figures: Chapter 5: Techniques

Use this file to turn each diagram into your own explanation. Rendered page images live in `../../artifacts/book/figure-pages/` after `make extract`.

- [ ] PDF page 125: Figure 5.1: Floating-point number formats have both exponent and mantissa bits along with the sign bit.
- [ ] PDF page 129: Figure 5.2: Quantization risk is low for weights and activations, moderate for KV cache, and high for attention.
- [ ] PDF page 132: Figure 5.3: Speculative decoding from draft token generation and validation to prefi x acceptance with subsequent token generation.
- [ ] PDF page 135: Figure 5.4: Medusa heads each generate a draft token on top of the token generated by the target model.
- [ ] PDF page 136: Figure 5.5: EAGLE speculative models take hidden states as input and produce draft tokens as output.
- [ ] PDF page 137: Figure 5.6: An n-gram dictionary matches prefi xes to likely suffixes and is especially useful for constrained languages like code.
- [ ] PDF page 138: Figure 5.7: A pair of four-token sequences with two-token matching prefi xes.
- [ ] PDF page 140: Figure 5.8: A pair of four-token sequences with no prefi x match, the fi rst tokens are different so it doesnt matter that the next three are the same.
- [ ] PDF page 141: Figure 5.9: Allocating free GPU memory to the KV cache is an essential confi guration decision when running inference engines.
- [ ] PDF page 142: Figure 5.10: Cache-aware routing allocates traffic based on KV cache rather than simply dividing requests evenly across replicas.
- [ ] PDF page 144: Figure 5.11: After fi guring out how much VRAM inference requires, round up to the next available instance size to determine minimum GPU count.
- [ ] PDF page 146: Figure 5.12: Tensor Parallelism splits weights across GPUs, effectively sharing VRAM resources to run large models fast.
- [ ] PDF page 146: Figure 5.13: For Mixture of Experts models, each expert runs across multiple GPUs with Tensor Parallelism.
- [ ] PDF page 147: Figure 5.14: Expert Parallelism runs each expert within a single GPU, with GPUs each hosting multiple experts.
- [ ] PDF page 148: Figure 5.15: This deployment uses TP for attention and EP for the sparse MoE layer. Expert Parallelism requires less inter-GPU communication than Tensor Par- allelism. The Expert Router, which determines which experts each token acti- vates, is replicated onto each GPU as it is a relatively small component of the model. Inter-GPU communication is necessary for passing tokens from expert
- [ ] PDF page 149: Figure 5.16: Infi niBand enables multi-node inference across more than eight GPUs via high-bandwidth node-to-node interconnect.
- [ ] PDF page 151: Figure 5.17: Disaggregation assigns prefi ll workers to generate the fi rst token and decode workers to generate subsequent tokens.
